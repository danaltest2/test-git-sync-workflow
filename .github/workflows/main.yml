name: Sync repository workflow

on:
  workflow_call:   
  
jobs:
  reusable_workflow_job:
    name: checkout test
    runs-on: self-hosted
    steps:
      - name: checkout
        uses: actions/checkout@v4
      - name: push to on-prem git repo
        run: |
          ls -la
          echo "push repo to on-prem git"
          git push http://${{ secrets.GITLAB_ACCESS_TOKEN_NAME }}:${{ secrets.GITLAB_ADDRESS }}@${{ secrets.GITLAB_ADDRESS }}/${{ secrets.GITLAB_GROUP }}/${{ github.repository }}.git ${{ github.ref_name	}}
